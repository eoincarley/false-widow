# Additional dependencies for raw data collection (optional)
# Only needed if re-fetching occurrence data and satellite imagery from APIs.
# Requires a Google Earth Engine account and authentication.
# Install with: pip install -r requirements-fetch.txt

-r requirements.txt

pygbif==0.6.6
pyinaturalist==0.21.0
pyinaturalist-convert==0.6.7
earthengine-api==1.3.1
geemap==0.34.5
